import { PLACEMENT } from './position';
declare type Props = {
    visible: boolean;
    placement: PLACEMENT;
};
export declare const PanelShadow: ({ visible, placement }: Props) => JSX.Element;
export {};
